# RoleLab Lite 交付说明

这是 Tai Labs `AI Engineer Intern — Full Stack` assessment 的完整候选交付包。核心取舍是做一个可运行、边界诚实的工作流实验原型，而不是再做一款通用聊天机器人或课程生成器。

便于转移的源码压缩包位于同级目录 `rolelab-submission.zip`；日常开发与发布请以本项目目录为准。

## 已完成

- `SUBMISSION.md`：可直接提交的英文书面答案，覆盖三个产品、受众、价值、架构、技术栈、数据、模型、部署、成本、可靠性、验证方法和有限的新颖性主张。
- `RoleLab Lite`：Next.js/TypeScript 工作原型，包含 synthetic evidence、可编辑双通道 artifact、辅助 sidecar、五个关键 gate、100 分 rubric、对照结果、分工图和训练 drill。
- `RESEARCH.md`：公司、产品、竞品和学术依据；所有时效性结论以 2026-09-12 为截止日。
- `DEMO_SCRIPT.md`：90 秒英文演示稿。
- `INTERVIEW.md`：founder 可能追问的问题与简洁回答。
- `docs/images/`：桌面 brief、workbench、通过、失败和手机版截图。

## 已验证

- `npm run check`：lint、typecheck、21/21 自动化测试全部通过。
- `npm run build`：Next.js production build 通过。
- 仅复制 Git staged files 的全新临时 checkout 中，`npm ci → npm run check → npm run build` 再次通过，排除了依赖本机生成文件的假阳性。
- Chromium production smoke test：guided happy path 为 `100/100 · Human review required`；违规承诺触发 G4/G5，结果为 `59/100 · Not deployable`。
- 刷新后可从 `localStorage` 恢复报告；390 px 宽度无横向溢出；浏览器控制台 0 error / 0 warning。

## 已上线

GitHub 仓库已经发布：`https://github.com/michaela-liu1225/rolelab-lite`。

Vercel 线上演示已经发布：`https://rolelab-lite.vercel.app`。

- 使用个人 Hobby 方案，从 GitHub `main` 自动部署。
- 公开站点未配置 `OPENAI_API_KEY`，清楚标识为 guided mode，避免匿名消耗模型额度。
- `/api/status` 返回 200，并显示 `Guided demo mode`。
- 线上正常路径得到 `100/100 · Human review required`。
- 线上违规承诺路径触发 G3/G4/G5，结果为 `Not deployable`。
- `SUBMISSION.md` 已写入正式 live URL。

## 最后提交

通过 assessment 私有链接提交以下两项：

1. Live demo：`https://rolelab-lite.vercel.app`
2. GitHub：`https://github.com/michaela-liu1225/rolelab-lite`

## 本地运行

```bash
npm install
npm run dev
```

生产模式验证：

```bash
npm run check
npm run build
npm run start
```

## 演示时要主动说清的边界

- 两条参考 arm 是明确标注的 illustrative seeded rows；只有当前 Harbor Analytics artifact 是本次实际计算结果。
- 无 key 时加载的是 reviewed example，不伪装成模型输出；`Challenge` 分析当前文字而不替换它。
- 当前 rules evaluator 只覆盖该 case 的已支持模式，不是通用语义安全证明；通过时显示 `Human review required`，不显示自动批准。
- 真正生产版会把关键决策改成 typed fields 的确定性验证，自由文本只承担沟通质量评分。
- 新颖性主张只针对“工作流导入 → 分步骤人机实验 → 分工与控制规则 → 定向训练/再认证”的闭环组合，不声称单个组件原创。
